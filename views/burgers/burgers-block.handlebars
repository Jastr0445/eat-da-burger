<li>
	{{name}}

	<button class="change-add" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if added}}{{else}}devoured{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">devoured!</button>
</li>
